---
description: 安全规范 - 认证授权、输入校验、SQL注入防护、敏感数据、接口安全
globs: "**/*.{java,py,vue,ts}"
alwaysApply: false
---

# 安全规范

## 认证授权
- 敏感接口需登录验证
- 使用 JWT Token 认证
- Token 存放在 Authorization 头部
- 接口权限控制到按钮级别

## 输入校验
- 所有用户输入必须校验
- 使用框架验证注解或库
- 敏感字符转义处理
- 文件上传校验类型和大小

## SQL 注入防护
- 使用参数化查询
- 禁止字符串拼接 SQL
- MyBatis 使用 #{} 而非 ${}

## 敏感数据
- 密码使用 BCrypt 加密
- 敏感字段接口不返回
- 日志不记录敏感信息
- 接口返回数据脱敏处理

## 接口安全
- 高频接口添加限流
- 敏感操作记录审计日志
- 防止 CSRF 攻击
- 防止 XSS 攻击

## 传输安全
- 生产环境使用 HTTPS
- 配置安全请求头
- CORS 合理配置
