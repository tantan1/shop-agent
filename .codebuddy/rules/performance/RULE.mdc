---
description: 性能优化规范 - 数据库优化、缓存、异步处理、前端优化
globs: "**/*.{java,py,vue,ts}"
alwaysApply: false
---

# 性能优化规范

## 数据库优化
- 避免使用 SELECT * 查询，指定具体列
- 批量操作替代循环单条操作
- 合理创建索引，避免过多索引
- 避免 N+1 查询，使用 JOIN 或预加载
- 慢查询日志监控和优化

## 缓存使用
- 热点数据使用 Redis 缓存
- 设置合理过期时间
- 缓存 key 命名规范统一
- 数据更新时及时清除缓存

## 异步处理
- 耗时操作使用异步方式
- 避免同步阻塞调用
- 消息队列处理非即时任务
- 邮件短信等使用异步发送

## 前端优化
- 路由懒加载
- 图片懒加载和压缩
- 减少 HTTP 请求
- 使用 CDN 加速静态资源

## 代码优化
- 避免创建过多临时对象
- 合理使用连接池
- 及时释放资源
- 避免重复计算
