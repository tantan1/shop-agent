---
description: 缓存规范 - 缓存策略、Key命名、过期时间、数据一致性
globs: "**/*.{java,py}"
alwaysApply: false
---

# 缓存规范

## 缓存使用场景
- 热点数据
- 不经常变化的数据
- 计算结果复杂的数据

## 缓存策略
- 读多写少：Cache-Aside
- 写多读少：Write-Through
- 实时性要求高：不使用缓存

## Key 命名
- 格式：项目:模块:业务:唯一标识
- 示例：shop:user:info:1001

## 过期时间
- 短期缓存：分钟级别
- 中期缓存：小时级别
- 长期缓存：天级别
- 根据业务调整

## 数据一致性
- 更新时先删缓存再更新数据库
- 使用分布式锁保证原子性
- 考虑双删策略

## 注意事项
- 避免缓存穿透：布隆过滤器
- 避免缓存击穿：互斥锁
- 避免缓存雪崩：随机过期时间
