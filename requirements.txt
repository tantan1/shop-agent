fastapi[all]
sqlalchemy[asyncio]
aiomysql
pydantic-settings
python-multipart
python-jose[cryptography]
passlib[bcrypt]
structlog
colorama
langchain==1.2.15
langchain-experimental>=0.3.0
langchain-openai
langchain-community==0.4.1
langfuse>=4.7.0
python-dotenv>=1.0.0

pymilvus==2.6.12
pgvector==0.4.2       # PostgreSQL pgvector 向量存储（可选，VECTOR_STORE_PROVIDER=pgvector 时使用）
psycopg2-binary>=2.9   # PostgreSQL 驱动（pgvector 依赖）
dashscope
httpx
sentence-transformers
FlagEmbedding>=1.3.0
volcengine-python-sdk[ark]
redis>=4.5.0

torch>=2.3.0
# --- 如不需要 GPU 或 CUDA 版本不同，请将上一行 torch 改为 torch --index-url https://download.pytorch.org/whl/cpu ---
numpy>=1.21.0
faiss-cpu
prometheus-fastapi-instrumentator>=7.0.0
prometheus-client>=0.21.0
docling==2.92.0
rapidocr-onnxruntime
ragas>=0.2.0
datasets>=3.0.0
pandas>=2.0.0
jieba  # 中文分词，用于 RAGAS 评估脚本关键词重叠率计算
tokenizers>=0.19.0  # HF Rust tokenizer，轻量 Token 预估（无 torch 依赖）
bitsandbytes>=0.43.0  # 本地小模型 4bit 量化（节省约 75% 显存）
apache-skywalking>=1.0.0  # 分布式链路追踪（APM），1.x API 与 0.x 不兼容
mcp>=1.0.0  # MCP 协议 SDK，用于将 Skill 体系标准化暴露为 Agent-to-Tool 协议